#!/bin/bash
set -euo pipefail

SCRIPT_DIR="$(CDPATH= cd -- "$(dirname -- "$0")" && pwd)"
if (( $# == 0 )); then
  set -- status
fi
exec /usr/bin/python3 "$SCRIPT_DIR/github_project_todo_monitor.py" \
  --config "$SCRIPT_DIR/config.json" "$@"
